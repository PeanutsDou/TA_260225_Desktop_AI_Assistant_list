# 此目录已废弃

原有的远程聊天服务端代码 `server_app.py` 已被备份为 `server_app.py.bak`。

新的服务端代码已集成到项目根目录下的 **云端部署包** 生成逻辑中。
请参考根目录下的 `prepare_server_package.py` 和生成的 `server_dist/` 目录。

如果需要恢复旧版逻辑，请将 `server_app.py.bak` 重命名回 `server_app.py`。
